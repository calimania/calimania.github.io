---
import Layout from '~/layouts/PageLayout.astro';

const metadata = {
  title: 'Code of Conduct',
  redirect: '/about/code-of-conduct'
};
---

<Layout metadata={metadata} redirect={metadata.redirect}>
</Layout>
